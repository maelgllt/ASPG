name: flutter_supabase_twitter_sign_in
description: A Flutter application that demonstrates how to sign in with Twitter using Supabase.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_auth_ui: ^0.4.0+1
  langchain_openai: ^0.4.0

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter

assets:
  - assets/

